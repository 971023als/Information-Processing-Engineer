# WAS(Web Application Server)

### 8-3 WAS
- 사용자 요청 스레드를 처리하고, 데이터베이스에 접속하여 SQL 쿼리 문에 대한 결과 값을 반환하는 역할을 수행하는 서버이다.

---

# JVM(Java Virtual Machine)

### 8-5 JVM
- 시스템 메모리를 관리하면서 자바 기반 애플리케이션을 위해 이식 가능한 실행 환경을 제공하는 소프트웨어이다.

---

# 컨테이너(Container)

### 컨테이너
- JSP와 서블릿을 실행시킬 수 있는 소프트웨어이다.

---

# 형상 관리

### 8-6 베이스라인(Baseline)
- 개발 과정의 각 단계의 산출물을 검토, 평가, 조정, 처리 등 변화를 통제하는 시점의 기준이다.

### 형상통제위원회(CCB; Configuration Control Board)
- 형상 관리에 대한 주요 방침을 정하고 산출물을 검토하며, 단계별 의사결정을 수행하는 조직이다.

---

# Java 및 빌드 도구

### 8-8 JDK(Java Development Kit)
- 자바 애플리케이션을 구축하기 위한 핵심 플랫폼

### Maven(메이븐)
- Java 기반 프로젝트 관리를 목적으로 하는 빌드 도구
- 컴파일, 빌드, 패키지, 테스트, 라이브러리 관리 기능 제공
- **porm.xml** 활용

### Gradle(그래들)
- Groovy 기반의 빌드 도구
- Ant와 Maven 등 이전 세대 빌드 도구의 단점을 보완 및 개선

---

# 브랜치(Branch)

### 브랜치
- 여러 개발자들이 동시에 다양한 작업을 할 수 있게 만들어 주는 기능
- 독립적인 작업 영역(저장소)에서 소스 코드를 자유롭게 변경 가능

---

# 추상화와 공통 모듈

### 추상화(Abstraction)
- 공통의 속성이나 기능을 묶어 이름을 붙이는 것을 의미

### 공통 모듈(Common Module)
- 전체 프로그램의 기능 중 특정 기능을 처리할 수 있는 실행 코드
- 자체적으로 컴파일 가능하며, 다른 프로그램에서 재사용 가능

---

# DAO, VO, DTO

### DAO(Data Access Object)
- 특정 타입의 데이터베이스에 추상 인터페이스를 제공하는 객체
- 세부 내용 노출 없이 데이터를 조작하는 객체

### VO(Value Object)
- 간단한 엔티티를 의미하는 작은 객체
- 가변 클래스인 DTO와 달리 고정 클래스를 가진 객체

### DTO(Data Transfer Object)
- 프로세스 사이에서 데이터를 전송하는 객체
- 데이터 저장 및 회수 외 다른 기능이 없는 객체

---

# 서비스(Service)

### 서비스
- 사용자의 요청을 처리하는 로직을 구현
- DAO 클래스를 통해 DB 연동 기능을 수행

---

# 테스트 기법

### 화이트 박스(White Box) 기법
- 프로그램의 로직을 이해하고 내부 구조와 동작을 검사하는 소프트웨어 테스트 방식

### 8-18 메서드(Method)
- 클래스에서 정의된 여러 변수를 사용하여 정해진 기능을 실행
- 다른 곳에서 인자를 주어 호출하거나, 정해진 자료형을 반환 가능

### Junit
- Java 프로그래밍 언어용 단위 테스트 도구

---

# 개발

### 8-19 어노테이션(Annotation)
- 자바 코드에 주석을 달아 특별한 의미를 부여한 메타데이터
- 보통 `@` 기호를 앞에 붙여서 사용

---

# 백엔드와 프론트엔드

### 8-26 백엔드(Back-End)
- 사용자와 만나지 않고 프론트엔드와 연동하여 핵심 로직을 처리하는 영역
- DB나 인터페이스를 통해 시스템에 접근

### 프론트엔드(Front-End)
- 사용자의 화면에 나타나는 웹 화면 영역
- JSP, 자바스크립트, CSS, HTML, Node.js, React.js, Angular.js 등 기술 활용

---

# MyBatis

### 8-27 MyBatis
- Java의 관계형 DB 프로그래밍을 쉽게 할 수 있도록 도와주는 개발 프레임워크

---

# DAO vs DTO vs VO

### DAO(Data Access Object)
- 특정 타입의 데이터베이스에 추상 인터페이스를 제공하며, 세부 내용 노출 없이 데이터를 조작

### DTO(Data Transfer Object)
- 프로세스 간 데이터를 전송하는 객체로, 데이터 저장 및 회수 외 다른 기능이 없음

### VO(Value Object)
- 간단한 엔티티를 의미하는 작은 객체로, DTO와 달리 고정 클래스를 가짐
